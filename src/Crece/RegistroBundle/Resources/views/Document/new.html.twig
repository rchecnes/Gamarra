{% extends '::base.html.twig' %}

{% block body -%}
<form action="{{ path('document_create') }}" method="POST" {{ form_enctype(form) }}>
   <div class="row">
      <div class="col-md-3">{{ form_row(form.name) }}</div>
      <div class="col-md-3">{{ form_row(form.file) }}</div>
      <div>{{ form_row(form._token) }}</div>
   </div>
	<p class="pull-right">
	 <input type="submit" value="Guardar" class="btn-small btn-primary btn" />
	 <input type="button" value="Cancelar" class="btn-small btn" onclick="location.href='{{ path('document')}}'" />
	</p>
</fomr>
{% endblock %}
